server{
  http{
    port=":8080"
  }
  thrift{
    port=":8084"
  }
  admin{
    disable=true
  }
  service{
    useDB=true
    database {
        host="localhost"
        port=3306
        user="root"
        password="root"
    }
  }
}